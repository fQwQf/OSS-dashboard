# 华中科技大学开放原子开源俱乐部活动仪表板 - 项目报告

## 项目概述

本项目是一个专门用于监控和可视化 **华中科技大学开放原子开源俱乐部 (hust-open-atom-club)** 活动的数据仪表板系统。项目采用前后端分离架构，实现了从数据采集、存储、聚合到可视化的完整流程。

## 主要完成工作

### 1. 系统架构设计

- **后端服务**: 基于 Node.js + Express.js 构建 RESTful API 服务
- **前端应用**: 使用 React + Vite 构建响应式 Web 界面
- **数据库**: PostgreSQL 存储结构化数据
- **缓存层**: Redis 提供数据缓存，提升查询性能
- **任务调度**: 使用 node-cron 实现定时数据采集任务

### 2. 数据库设计

设计了完整的三层数据模型：

- **组织级别** (`activity_snapshots`): 存储组织整体活动快照
- **SIG 级别** (`sig_snapshots`): 存储各 Special Interest Group 的聚合数据
- **仓库级别** (`repo_snapshots`): 存储单个仓库的详细活动数据

建立了 SIG 与仓库的关联关系，支持按 SIG 进行数据聚合和统计。

### 3. 数据采集功能

实现了双管道数据采集机制：

#### 管道 1: Git 提交统计
- 本地克隆/更新 GitHub 仓库到 `repos/` 目录
- 使用 `git log` 统计每日提交数量、新增/删除代码行数
- 支持并发处理（5个并发），不受 API 限流影响

#### 管道 2: GitHub API 数据采集
- 通过 GitHub Search API 获取 PR 和 Issue 数据
- 统计新增/关闭的 PR 和 Issue 数量
- 识别活跃贡献者
- 实现 API 速率限制处理（每次请求间隔 2 秒，3个并发）
- 支持自动重试和指数退避策略

### 4. 数据聚合功能

实现了三级数据聚合：

1. **仓库 → SIG**: 将同一 SIG 下的所有仓库数据聚合
2. **SIG → 组织**: 将所有 SIG 数据聚合为组织总览
3. **时间序列**: 支持按日期范围查询历史趋势数据

### 5. API 接口实现

提供了完整的 RESTful API：

- `GET /api/v1/organization/sigs` - 获取所有 SIG 列表
- `GET /api/v1/sig/:sigId/timeseries` - 获取 SIG 时间序列数据
- `GET /api/v1/sig/:sigId/timeseries/commits` - 获取 SIG Commit 数据
- `GET /api/v1/sig/:sigId/timeseries/api` - 获取 SIG API 数据
- `GET /api/v1/organization/timeseries` - 获取组织时间序列数据
- `GET /api/v1/organization/latest-activity` - 获取最新活动列表（支持分页）

所有接口支持 Redis 缓存（10分钟 TTL），提升响应速度。

### 6. 前端可视化

- **组织总览卡片**: 展示最新活动快照（PR、Issue、Commit、代码行数等）
- **SIG 选择器**: 支持选择不同 SIG 查看其活动趋势
- **趋势图表**: 使用 ECharts 展示多维度活动趋势（PR、Issue、Commit、代码行数）
- **最新活动列表**: 展示组织范围内的最新 PR 和 Issue，支持分页浏览

### 7. 数据初始化

- 创建了 6 个 SIG 的初始数据：
  - 镜像站运维 SIG
  - Linux内核SIG
  - RUSTSBI SIG
  - HCTT SIG
  - Dojo SIG
  - 数字基础设施维护 SIG
- 配置了 100+ 个仓库与 SIG 的关联关系
- 实现了历史数据回填功能（默认回填最近 30 天数据）

### 8. 工具脚本

创建了数据重建脚本 `rebuild-aggregations.js`，用于：
- 从仓库快照重新聚合 SIG 和组织级别数据
- 修复数据不一致问题
- 验证聚合结果的正确性

### 9. 性能优化

- **并发控制**: Git 操作和 API 操作分别设置不同的并发限制
- **缓存策略**: Redis 缓存减少数据库查询压力
- **API 限流处理**: 智能处理 GitHub API 速率限制，避免请求失败
- **数据去重**: 使用 `ON CONFLICT` 确保数据幂等性

### 10. 错误处理与容错

- Git 操作失败时返回零值，不影响整体流程
- API 请求失败时自动重试（最多 3 次，指数退避）
- 空仓库和无效仓库的优雅处理
- 完善的日志输出，便于问题排查

## 技术亮点

1. **双管道架构**: 将 Git 统计和 API 采集分离，提高效率和可靠性
2. **三级数据模型**: 仓库 → SIG → 组织，支持灵活的查询和聚合
3. **智能限流**: 自动处理 GitHub API 速率限制，确保数据采集的稳定性
4. **缓存优化**: Redis 缓存显著提升 API 响应速度
5. **数据一致性**: 使用数据库约束和幂等操作确保数据准确性

## 项目文件结构

```
oss-dashboard/
├── backend/              # Node.js 后端服务
│   ├── server.js        # 主服务器文件（API、Cron、Git 逻辑）
│   └── scripts/         # 工具脚本
├── frontend/            # React 前端应用
│   └── src/
│       └── App.jsx      # 主应用组件
├── db/                  # 数据库脚本
│   ├── schema.sql       # 表结构定义
│   ├── seed.sql         # 初始数据
│   └── public.sql       # 生产数据导出
└── repos/               # Git 仓库本地存储
```

## 遇到的问题与解决方案

### 问题描述

在系统部署初期，发现前端仪表板无法显示任何数据，所有数据卡片显示为空或"..."状态，图表也无法加载，导致整个系统看起来完全不工作。

### 问题原因分析

经过排查，发现问题的根本原因是：

1. **数据未写入数据库**
   - 初期部署时，数据采集任务虽然在运行，但数据没有成功写入数据库
   - 可能的原因包括：
     - 数据库连接配置问题
     - 数据库表结构未正确初始化
     - SQL 语句执行失败但未被及时发现
     - 事务处理或约束冲突导致数据回滚
   - 前端 API 查询数据库时返回空数据或旧数据

2. **日志信息不够明确**
   - 数据采集日志显示"采集到"数据，但未明确标注"写入数据库成功"
   - 数据库写入失败时，错误信息可能被其他日志淹没
   - 缺少针对数据库操作的专门验证步骤

3. **验证机制缺失**
   - 没有在采集完成后主动验证数据库中的数据
   - 无法快速发现数据写入失败的问题
   - 用户只能通过前端表现来判断系统是否正常工作

### 采取的调整措施

#### 1. 排查问题根源
- **检查日志输出**：
  - 分析后端控制台日志，发现数据采集过程有运行
  - 查看"[Git Pipeline]"和"[API Pipeline]"的输出
  - 发现数据采集日志显示正常，但数据库中查询不到数据

- **验证数据库状态**：
  - 直接查询数据库表，确认数据确实未写入
  - 检查表结构和约束是否正确
  - 确认数据库连接正常

- **定位逻辑问题**：
  - 发现数据采集和聚合的逻辑存在问题
  - 日志判断条件可能不够准确
  - 数据流转过程中存在断点

#### 2. 修改 server.js 核心逻辑

- **优化聚合逻辑**：
  - 修改 `aggregateSigSnapshot()` 函数的数据聚合逻辑
  - 确保仓库级数据能正确聚合到 SIG 级别
  - 修复 SIG 数据向组织级别聚合的流程
  - 调整聚合查询的 SQL 语句，确保数据完整性

- **改进日志判断**：
  - 增强日志输出的详细程度和准确性
  - 在关键节点添加数据库写入成功/失败的明确标识
  - 改进错误捕获和异常处理机制
  - 添加更多中间状态的日志输出，便于追踪数据流

- **修复数据写入流程**：
  - 检查并修正 `ON CONFLICT` 语句的使用
  - 确保数据库事务正确提交
  - 优化并发处理中的数据库操作顺序
  - 修复可能导致数据丢失的边界情况

#### 3. 测试和验证修复效果

- **重启服务测试**：
  - 修改代码后重启后端服务
  - 观察新的日志输出，确认修改生效
  - 验证数据采集和聚合流程是否正常

- **数据库验证**：直接查询数据库确认数据是否正确写入
  ```sql
  -- 检查组织快照最新日期
  SELECT MAX(snapshot_date) FROM activity_snapshots;
  
  -- 检查各表的数据量
  SELECT COUNT(*) FROM repo_snapshots;
  SELECT COUNT(*) FROM sig_snapshots;
  SELECT COUNT(*) FROM activity_snapshots;
  
  -- 查看最新的几条记录
  SELECT * FROM activity_snapshots ORDER BY snapshot_date DESC LIMIT 5;
  ```

- **前端验证**：
  - 清除 Redis 缓存，确保获取最新数据
  - 刷新前端页面，确认数据正常显示
  - 验证所有数据卡片和图表都能正确加载

#### 4. 持续监控和优化

- **日志监控**：
  - 持续观察后端日志，确保数据采集稳定
  - 关注"✅ 已存储到数据库"等成功标识
  - 及时发现和处理异常情况

- **定期验证**：
  - 定期检查数据库中的数据完整性
  - 验证各级聚合数据的一致性
  - 确保定时任务正常运行

### 最佳实践建议

1. **部署前检查**：
   - 确认数据库服务运行正常
   - 验证 `.env` 配置文件的所有参数
   - 执行 `schema.sql` 初始化表结构
   - 执行 `seed.sql` 填充初始数据

2. **首次启动**：
   - 启动后台服务并密切关注日志输出
   - 查看是否有数据库连接错误或 SQL 执行失败
   - 等待 2-4 小时让数据采集任务完成
   - 使用 SQL 查询验证数据是否正确写入

3. **问题排查**：
   - 如果前端无数据，首先检查数据库而非缓存
   - 查看后端日志确认数据写入成功消息
   - 必要时清除 Redis 缓存并重启服务

4. **日常运维**：
   - 配置 Cron 任务每 6 小时自动更新数据
   - 定期检查数据库磁盘空间和连接池状态
   - 监控 GitHub API 限流情况
   - 备份数据库数据

5. **性能优化**：
   - 根据网络环境调整 API 请求延迟
   - 优化并发数以平衡速度和稳定性
   - 合理设置 Redis 缓存过期时间

### 调整成果

通过修改 `server.js` 中的聚合逻辑和日志判断方法：
- ✅ 成功解决了数据无法写入数据库的核心问题
- ✅ 优化了数据采集、聚合、存储的完整流程
- ✅ 改进了日志输出机制，提高了问题排查效率
- ✅ 增强了错误处理和异常捕获能力
- ✅ 系统现已稳定运行，前端可正常显示完整数据
- ✅ 积累了宝贵的问题排查和代码调试经验

## 总结

本项目成功构建了一个功能完整、性能优化的开源活动监控仪表板系统。通过合理的数据模型设计、高效的数据采集机制和完善的前端可视化，为华中科技大学开放原子开源俱乐部提供了全面的活动数据监控和分析能力。

在实际部署过程中，通过深入理解系统架构和数据流动机制，成功解决了数据采集进度可见性和缓存管理的问题，确保了系统的稳定运行。

系统已具备生产环境部署条件，支持自动化的数据采集、聚合和展示，能够有效帮助组织了解各 SIG 的活动情况和整体发展趋势。建议在首次部署时预留足够的数据采集时间，并通过日志监控确保任务顺利完成。

